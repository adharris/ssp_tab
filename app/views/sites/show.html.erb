<dl>
  <dt>State</dt>
  <dd><%= @site.state %></dd>
  <dt>Description</dt>
  <dd><%= @site.description %></dd>
</dl>

<% if can? :edit, @site %>
  <%= link_to "Edit site", edit_site_path(@site) %>
<% end %>

<%= render 'programs/table', :programs => @site.programs %>

